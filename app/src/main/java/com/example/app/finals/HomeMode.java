package com.example.app.finals;

/**
 * Enum describing the possible user's mode in a
 * home request
 */
public enum HomeMode {
    //just see where the last set home is
    viewMode,
    //set the home in his current location
    setMode;
}
